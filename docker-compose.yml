services:
  couchdb:
    image: couchdb:3
    container_name: couchdb
    restart: unless-stopped
    ports:
      - "5984:5984"
    environment:
      COUCHDB_USER: admin
      COUCHDB_PASSWORD: pass
    volumes:
      - couchdb_data:/opt/couchdb/data

volumes:
  couchdb_data:
